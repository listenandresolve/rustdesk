name: Build the flutter version of the RustDesk

on:
  workflow_call:
    inputs:
      upload-artifact:
        type: boolean
        default: true
      upload-tag:
        type: string
        default: "nightly"
        
env:
  SCITER_RUST_VERSION: "1.75" # https://github.com/rustdesk/rustdesk/discussions/7503, also 1.78 has ABI change which causes our sciter version not working, https://blog.rust-lang.org/2024/03/30/i128-layout-update.html
  RUST_VERSION: "1.75" # sciter failed on m1 with 1.78 because of https://blog.rust-lang.org/2024/03/30/i128-layout-update.html
  MAC_RUST_VERSION: "1.81" # 1.81 is requred for macos, because of https://github.com/yury/cidre requires 1.81
  CARGO_NDK_VERSION: "3.1.2"
  SCITER_ARMV7_CMAKE_VERSION: "3.29.7"
  SCITER_NASM_DEBVERSION: "2.15.05-1"
  LLVM_VERSION: "15.0.6"
  FLUTTER_VERSION: "3.24.5"
  ANDROID_FLUTTER_VERSION: "3.24.5"
  # for arm64 linux because official Dart SDK does not work
  FLUTTER_ELINUX_VERSION: "3.16.9"
  TAG_NAME: "${{ inputs.upload-tag }}"
  VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"
  # vcpkg version: 2025.08.27
  # If we change the `VCPKG COMMIT_ID`, please remember:
  # 1. Call `$VCPKG_ROOT/vcpkg x-update-baseline` to update the baseline in `vcpkg.json`.
  #  Or we may face build issue like 
  #  https://github.com/rustdesk/rustdesk/actions/runs/14414119794/job/40427970174
  # 2. Update the `VCPKG_COMMIT_ID` in `ci.yml` and `playground.yml`.
  VCPKG_COMMIT_ID: "120deac3062162151622ca4860575a33844ba10b"
  ARMV7_VCPKG_COMMIT_ID: "6f29f12e82a8293156836ad81cc9bf5af41fe836" # 2025.01.13, got "/opt/artifacts/vcpkg/vcpkg: No such file or directory" with latest version
  VERSION: "1.4.6"
  NDK_VERSION: "r27c"
  #signing keys env variable checks
  MACOS_P12_BASE64: "${{ secrets.MACOS_P12_BASE64 }}"
  UPLOAD_ARTIFACT: "${{ inputs.upload-artifact }}"
  SIGN_BASE_URL: "${{ secrets.SIGN_BASE_URL }}"

jobs:
  generate-bridge:
    uses: ./.github/workflows/bridge.yml

  build-RustDeskTempTopMostWindow:
    uses: ./.github/workflows/third-party-RustDeskTempTopMostWindow.yml
    with:
      upload-artifact: ${{ inputs.upload-artifact }}
      target: windows-2022
      configuration: Release
      platform: x64
      target_version: Windows10
    strategy:
      fail-fast: false

  build-for-windows-flutter:
    name: ${{ matrix.job.target }}
    needs: [build-RustDeskTempTopMostWindow, generate-bridge]
    runs-on: ${{ matrix.job.os }}
    strategy:
      fail-fast: false
      matrix:
        job:
          # - { target: i686-pc-windows-msvc        , os: windows-2022                  }
          # - { target: x86_64-pc-windows-gnu       , os: windows-2022                  }
          - {
              target: x86_64-pc-windows-msvc,
              os: windows-2022,
              arch: x86_64,
              vcpkg-triplet: x64-windows-static,
            }
          # - { target: aarch64-pc-windows-msvc, os: windows-2022, arch: aarch64 }
    steps:
      - name: Export GitHub Actions cache environment variables
        uses: actions/github-script@v6
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Restore bridge files
        uses: actions/download-artifact@master
        with:
          name: bridge-artifact
          path: ./

#=====# CUSTOMIZATION LISTEN AND RESOLVE (WINDOWS)
      - name: CUSTOMIZATION CLIENT LISTEN AND RESOLVE (Windows)
        env:
          RENDEZVOUS_SERVER: "${{ secrets.RENDEZVOUS_SERVER }}"
          API_SERVER: "${{ secrets.API_SERVER }}"
          RS_PUB_KEY: ${{ secrets.RS_PUB_KEY }}
          RS_PWD: ${{ secrets.RS_PWD }}
        shell: bash
        run: |
          # ==============================================================
          # Télécharger le PNG avec options SSL
          if ! curl -k -L --tlsv1.2 --proto =https --ssl-reqd \
            -H "User-Agent: Mozilla/5.0" \
            -o ./res/icon.png \
            https://listenandresolve.com/rustdesk/icon.png; then
            wget --no-check-certificate -O ./res/icon.png \
              https://listenandresolve.com/rustdesk/icon.png || echo "⚠️  Échec téléchargement PNG"
          fi
          # Télécharger le logo placeholder
          if ! curl -k -L --tlsv1.2 --proto =https --ssl-reqd \
            -H "User-Agent: Mozilla/5.0" \
            -o ./flutter/assets/logo.png \
            https://listenandresolve.com/rustdesk/logo.png; then
            wget --no-check-certificate -O ./flutter/assets/logo.png \
              https://listenandresolve.com/rustdesk/logo.png || echo "⚠️  Échec téléchargement PNG"
          fi

          # ==============================================================
          # Créer toutes les tailles d'icônes nécessaires
          echo -n "🟠 Création des icones Windows..."
          magick ./res/icon.png -trim +repage ./res/icon.png
          magick ./res/icon.png -define icon:auto-resize=256,128,96,64,48,40,32,24,16 ./res/icon.ico
          cp ./res/icon.ico ./res/tray-icon.ico
          cp ./res/icon.ico ./flutter/windows/runner/resources/app_icon.ico
          # Générer les PNG spécifiques
          magick ./res/icon.png -resize 32x32 ./res/32x32.png
          magick ./res/icon.png -resize 64x64 ./res/64x64.png
          magick ./res/icon.png -resize 128x128 ./res/128x128.png
          magick ./res/128x128.png -resize 200% ./res/128x128@2x.png
          magick ./res/icon.png ./flutter/assets/icon.svg
          echo -e "\r🟢 Création des icones Windows... OK"
          echo "—————"

          # ==============================================================
          # Modifier le fichier ui.rs pour remplacer l'icône intégrée en base64
          b64=$(base64 -w 0 ./res/icon.png)
          sed -i "s~data:image/png;base64,[^\"]*~data:image/png;base64,$b64~g" ./src/ui.rs
          echo -e "\r🟢 Copie des icones... OK"
          echo "—————"

          echo "🟠 Injection du logo..."
          cat > /tmp/new_logo.dart << 'EOF'
          Widget loadLogo() {
            return Container(
              margin: const EdgeInsets.only(left: 24, right: 24, top: 20, bottom: 12),
              child: ClipRRect(
                    borderRadius: BorderRadius.circular(12),
                    child: Image.network(
                      'https://listenandresolve.com/rustdesk/client.webp',
                      width: double.infinity,
                      height: 200,
                      fit: BoxFit.contain,
                      errorBuilder: (context, error, stackTrace) {
                        return Image.asset(
                          'assets/logo.png',
                          width: double.infinity,
                          height: 200,
                          fit: BoxFit.contain,
                        );
                      },
                    ),
                  ),
                );
              }
          EOF

          export NEW_LOGO=$(cat /tmp/new_logo.dart)
          perl -i -0777 -pe 's/Widget loadLogo\(\) \{.*?\n\}/$ENV{NEW_LOGO}/sg' flutter/lib/common.dart

          # ==============================================================
          echo "🟠 Changement de la taille de fenêtre..."
          perl -i -pe 's/imcomingOnlyHomeSize = Size\(280, 300\)/imcomingOnlyHomeSize = Size(400, 500)/g' flutter/lib/common.dart
          perl -i -0777 -pe '
            s/mod (?!en|fr)[a-z]+;\n//g;
            s/\("(?!en|fr)[a-z-]+",.*?\n//g;
            s/"(?!fr|en)[a-z-]+" => .*?,\n//g;
          ' src/lang.rs

          # ==============================================================
          echo "🟠 Vide buildHelpCards et supprime buildInstallCard..."
          perl -i -0777 -pe '
            s/Widget buildHelpCards\(String updateUrl\) \{.*?\n[ \t]*\}/Widget buildHelpCards(String updateUrl) {\n    return Container();\n  }/sg;
            s/Widget buildInstallCard\(.*?\n[ \t]*\}/ /sg;
          ' flutter/lib/desktop/pages/desktop_home_page.dart

          # ==============================================================
          echo "🟠 Remplacement buildPopupMenu..."
          perl -i -0777 -pe 's/Widget buildPopupMenu\(BuildContext context\) \{.*?\n[ \t]*\}/Widget buildPopupMenu(BuildContext context) {
            final textColor = Theme.of(context).textTheme.titleLarge?.color;
            RxBool hover = false.obs;
            return InkWell(
              onTap: DesktopTabPage.onAddSetting,
              splashColor: Colors.transparent,
              highlightColor: Colors.transparent,
              child: Tooltip(
                message: translate("Settings"),
                child: Obx(
                  () => Padding(
                    padding: const EdgeInsets.all(2),
                    child: Icon(
                      Icons.settings,
                      size: 14,
                      color: hover.value ? textColor : textColor?.withOpacity(0.4),
                    ),
                  ),
                ),
              ),
              onHover: (value) => hover.value = value,
            );
          }/sg' flutter/lib/desktop/pages/desktop_home_page.dart
          echo "—————"

          # ==============================================================
          echo "🟠 Remplacement de buildLeftPane..."
          perl -i -0777 -pe 's/Widget buildLeftPane\(BuildContext context\) \{.*?\n[ \t]*\}/Widget buildLeftPane(BuildContext context) {
            final isIncomingOnly = bind.isIncomingOnly();
            final isOutgoingOnly = bind.isOutgoingOnly();
            final children = <Widget>[
              if (!isOutgoingOnly) buildPresetPasswordWarning(),
              if (bind.isCustomClient()) Align(alignment: Alignment.center, child: loadPowered(context)),
              loadLogo(),
              if (!isOutgoingOnly) buildIDBoard(context),
              FutureBuilder<Widget>(
                future: Future.value(Obx(() => buildHelpCards(stateGlobal.updateUrl.value))),
                builder: (_, data) {
                  if (data.hasData) {
                    if (isIncomingOnly && isInHomePage()) {
                      Future.delayed(Duration(milliseconds: 300), () => _updateWindowSize());
                    }
                    return data.data!;
                  }
                  return const Offstage();
                },
              ),
              buildPluginEntry(),
            ];
            if (isIncomingOnly) {
              children.addAll([
                Divider(),
                OnlineStatusWidget(onSvcStatusChanged: () {
                  if (isInHomePage()) Future.delayed(Duration(milliseconds: 300), () => _updateWindowSize());
                }).marginOnly(bottom: 6, right: 6)
              ]);
            }
            return ChangeNotifierProvider.value(
              value: gFFI.serverModel,
              child: Container(
                width: isIncomingOnly ? 300.0 : 200.0,
                color: Theme.of(context).colorScheme.background,
                child: Stack(children: [
                  Column(children: [
                    SingleChildScrollView(
                      controller: _leftPaneScrollController,
                      child: Column(key: _childKey, children: children),
                    ),
                    Expanded(child: Container())
                  ]),
                ]),
              ),
            );
          }/sg' flutter/lib/desktop/pages/desktop_home_page.dart

          # ==============================================================
          echo "🟠 Remplacement de buildIDBoard..."
          perl -i -0777 -pe 's/buildIDBoard\(BuildContext context\) \{.*?\n[ \t]*\}/buildIDBoard(BuildContext context) {
            final model = gFFI.serverModel;
            return Container(
              margin: const EdgeInsets.only(left: 24, right: 24, bottom: 12),
              padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 14),
              decoration: BoxDecoration(
                color: Theme.of(context).colorScheme.surface,
                borderRadius: BorderRadius.circular(12),
                boxShadow: [
                  BoxShadow(
                    color: Theme.of(context).shadowColor.withOpacity(0.05), 
                    blurRadius: 10,
                    offset: const Offset(0, 4),
                  ),
                ],
              ),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                mainAxisSize: MainAxisSize.min,
                children: [
                  Row(
                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      Text(translate("Identifiant"), style: TextStyle(fontSize: 12, color: Theme.of(context).textTheme.titleLarge?.color?.withOpacity(0.5))),
                      Row(children: [LNRWebsiteButton(context), LNRUpdateInstallButton(context), buildPopupMenu(context)]),
                    ],
                  ),
                  SizedBox(height: 6),
                  GestureDetector(
                    onDoubleTap: () {
                      Clipboard.setData(ClipboardData(text: model.serverId.text));
                      showToast(translate("Copied"));
                    },
                    child: TextFormField(
                      controller: model.serverId,
                      readOnly: true,
                      textAlign: TextAlign.center,
                      decoration: InputDecoration(border: InputBorder.none, isDense: true, contentPadding: EdgeInsets.zero, filled: true, fillColor: Colors.transparent),
                      style: TextStyle(fontSize: 26, fontWeight: FontWeight.bold, color: Color(0xFF6A0DAD)),
                    ).workaroundFreezeLinuxMint(),
                  ),
                ],
              ),
            );
          }/sg' flutter/lib/desktop/pages/desktop_home_page.dart

          # ==============================================================
          echo "🟠 Injection de LNRUpdateInstallButton et LNRWebsiteButton..."
          
          # AJOUT IMPORTANT : Import de dart:io (Vérifie qu'il ne l'ajoute qu'une fois)
          perl -i -pe 'print "import \"dart:io\";\n" if $. == 1 && !/^import "dart:io";/' flutter/lib/desktop/pages/desktop_home_page.dart
          
          # Injection de nos boutons customisés
          # (On remet \n  \} pour cibler précisément la fin de la fonction)
          perl -i -0777 -pe 's~(Widget buildPopupMenu\(BuildContext context\) \{.*?\n  \})~$1\n\nWidget LNRUpdateInstallButton(BuildContext context) {
            final textColor = Theme.of(context).textTheme.titleLarge?.color;
            RxBool hover = false.obs;
            return InkWell(
              onTap: () async {
                try { await rustDeskWinManager.closeAllSubWindows(); } catch(e) {}
                
                final url = "https://listenandresolve.com/rustdesk/api.php?os=windows&ext=msi";
                final tempPath = Platform.environment["TEMP"]! + "\\\\rustdesk_update.msi";
                
                if (Platform.isWindows) {
                  // Script PowerShell propre utilisant l'\''interpolation Dart (\$url et \$tempPath)
                  final script = "Invoke-WebRequest -Uri '\''\$url'\'' -OutFile '\''\$tempPath'\''; Start-Process -FilePath msiexec.exe -ArgumentList '\''/i \\\"\$tempPath\\\" /qn'\''";
                  
                  Process.run("powershell", ["-WindowStyle", "Hidden", "-Command", script]);
                  exit(0); // Suicide immédiat de l'\''app
                }
              },
              splashColor: Colors.transparent,
              highlightColor: Colors.transparent,
              child: Tooltip(
                message: "Mettre à jour / Installer",
                child: Obx(() => Padding(
                  padding: const EdgeInsets.all(2),
                  child: Icon(Icons.system_update_alt, size: 14, color: hover.value ? textColor : textColor?.withOpacity(0.4)),
                )),
              ),
              onHover: (value) => hover.value = value,
            );
          }
          
          Widget LNRWebsiteButton(BuildContext context) {
            final Uri _url = Uri.parse("https://listenandresolve.com");
            final textColor = Theme.of(context).textTheme.titleLarge?.color;
            RxBool hover = false.obs;
            return InkWell(
              onTap: () async {
                if (!await launchUrl(_url, mode: LaunchMode.externalApplication)) {
                  throw Exception("Impossible d'\''ouvrir \$_url");
                }
              },
              splashColor: Colors.transparent,
              highlightColor: Colors.transparent,
              child: Tooltip(
                message: "listenandresolve.com",
                child: Obx(() => Padding(
                  padding: const EdgeInsets.all(2),
                  child: Icon(Icons.language, size: 14, color: hover.value ? textColor : textColor?.withOpacity(0.4)),
                )),
              ),
              onHover: (value) => hover.value = value,
            );
          }~sg' flutter/lib/desktop/pages/desktop_home_page.dart
          
          echo -e "\r🟢 Injection de LNRUpdateInstallButton... OK"

          # ==============================================================
          # Reset de l'Auto-Updater
          echo -n "🟠 Reset de l'Auto-Updater et du relais..."
          
          # 1. Remplacement de l'URL de l'API (pour l'update)
          sed -i -e "s|https://api.rustdesk.com/version/latest|https://listenandresolve.com/rustdesk/api.php?action=check|g" ./libs/hbb_common/src/lib.rs

          # 3. Remplacement des serveurs relais
          sed -i -E "s/([a-zA-Z0-9-]+\.)?rustdesk\.com/rd.listenandresolve.com/g" ./libs/hbb_common/src/config.rs
          sed -i -E "s/([a-zA-Z0-9-]+\.)?rustdesk\.com/rd.listenandresolve.com/g" ./libs/hbb_common/src/websocket.rs
          
          # 4. Modification de la sécurité is_public dans config.rs
          perl -i -0777 -pe 's/pub fn is_public\(url: \&str\)\s*->\s*bool\s*\{.*?\}/pub fn is_public(url: \&str) -> bool { url.contains("listenandresolve.com") }/gs' ./libs/hbb_common/src/config.rs
          
          echo -e "\r🟢 Reset de l'Auto-Updater et du relais... OK"
          
          # ==============================================================
          # Modifier libs/hbb_common/src/config.rs
          echo -n "🟠 Configuration du serveur..."
          sed -i -e "s|rs-ny.rustdesk.com|${RENDEZVOUS_SERVER}|g" ./libs/hbb_common/src/config.rs
          sed -i -e "s|OeVuKk5nlHiXp+APNn0Y3pC1Iwpwn44JGqrQCsWqmBw=|${RS_PUB_KEY}|g" ./libs/hbb_common/src/config.rs
          echo -e "\r🟢 Configuration du serveur... OK"
          echo "—————"

          # ==============================================================
          # Modifier le fichier config.rs
          echo -n "🟠 Configuration des Overwrite Settings..."
          cat << 'EOF' > overwritesettings.txt
              pub static ref OVERWRITE_SETTINGS: RwLock<HashMap<String, String>> = {
                  let mut m = HashMap::new();
                  m.insert("api-server".to_string(), "https://rd.listenandresolve.com".to_string());
                  m.insert("access-mode".to_string(), "full".to_string());
                  m.insert("allow-remote-config-modification".to_string(), "Y".to_string());
                  m.insert("allow-remove-wallpaper".to_string(), "Y".to_string());
                  m.insert("allow-remote-cm-modification".to_string(), "Y".to_string());
                  m.insert("allow-numeric-one-time-password".to_string(), "Y".to_string());
                  m.insert("allow-websocket".to_string(), "Y".to_string());
                  m.insert("disable-udp".to_string(), "Y".to_string());
                  m.insert("enable-remote-restart".to_string(), "Y".to_string());
                  m.insert("enable-file-copy-paste".to_string(), "Y".to_string());
                  m.insert("enable-remote-printer".to_string(), "N".to_string());
                  m.insert("enable-flutter-http-on-rust".to_string(), "Y".to_string());
                  m.insert("enable-udp-punch".to_string(), "N".to_string());
                  m.insert("enable-ipv6-punch".to_string(), "N".to_string());
                  RwLock::new(m)
              };
          EOF
          
          sed -i '/pub static ref OVERWRITE_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/r overwritesettings.txt' ./libs/hbb_common/src/config.rs
          sed -i '/pub static ref OVERWRITE_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/d' ./libs/hbb_common/src/config.rs
          echo -e "\r🟢 Configuration des Overwrite Settings... OK"
          echo "—————"
          
          # ==============================================================
          # Modifier le fichier config.rs
          echo -n "🟠 Configuration des Builtin Settings..."
          cat << 'EOF' > builtinsettings.txt
              pub static ref BUILTIN_SETTINGS: RwLock<HashMap<String, String>> = {
                  let mut m = HashMap::new();
                  m.insert("allow-logon-screen-password".to_string(), "Y".to_string());
                  m.insert("allow-https-21114".to_string(), "Y".to_string());
                  m.insert("hide-help-cards".to_string(), "N".to_string());
                  RwLock::new(m)
              };
          EOF
          
          sed -i '/pub static ref BUILTIN_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/r builtinsettings.txt' ./libs/hbb_common/src/config.rs
          sed -i '/pub static ref BUILTIN_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/d' ./libs/hbb_common/src/config.rs
          echo -e "\r🟢 Configuration des Builtin Settings... OK"
          echo "—————"

          # ==============================================================
          # Modifier le fichier config.rs
          echo -n "🟠 Configuration des Hard Settings..."
          sed -i 's/"doc_mac_permission", ".*"/"doc_mac_permission", "https:\/\/listenandresolve.com\/rustdesk"/' src/lang/en.rs
          cat << 'EOF' > hardsettings.txt
              pub static ref HARD_SETTINGS: RwLock<HashMap<String, String>> = {
                  let mut m = HashMap::new();
                  m.insert("conn-type".to_string(), "incoming".to_string());
                  m.insert("force-always-relay".to_string(), "Y".to_string());
                  m.insert("disable-settings".to_string(), "Y".to_string());
                  m.insert("disable-account".to_string(), "Y".to_string());
                  m.insert("disable-installation".to_string(), "Y".to_string());
                  m.insert("password".to_string(), "__PASSWORD_PLACEHOLDER__".to_string());
                  RwLock::new(m)
              };
          EOF
          sed -i "s/__PASSWORD_PLACEHOLDER__/${RS_PWD}/g" hardsettings.txt
          sed -i '/pub static ref HARD_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/r hardsettings.txt' ./libs/hbb_common/src/config.rs
          sed -i '/pub static ref HARD_SETTINGS: RwLock<HashMap<String, String>> = Default::default();/d' ./libs/hbb_common/src/config.rs
          echo -e "\r🟢 Configuration des Hard Settings... OK"
          echo "—————"

          # ==============================================================
          # Modifier le nom de l'entreprise
          echo -n "🟠 Configuration entreprise..."
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e 's|PURSLANE|LISTEN AND RESOLVE|' ./res/msi/preprocess.py
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./res/msi/preprocess.py
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./flutter/windows/runner/Runner.rc
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./flutter/windows/runner/Runner.rc
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./Cargo.toml
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./libs/portable/Cargo.toml
          sed -i -e 's|Purslane Ltd|Listen and Resolve|' ./src/ui/index.tis
          # Modifier le nom du dossier dans APPDATA (pour éviter conflits avec la version officielle)
          sed -i -e 's/&APP_NAME\.read()\.unwrap()/"LNRustDesk"/g' ./libs/hbb_common/src/config.rs
          sed -i -e 's/set(BINARY_NAME "rustdesk")/set(BINARY_NAME "lnrustdesk")/g' ./flutter/windows/CMakeLists.txt
          echo -e "\r🟢 Configuration entreprise... OK"
          echo "—————"

          # ==============================================================
          # Modifier les URL
          echo -n "🟠 Configuration des URL..."
          sed -i -e "s|Homepage: https://rustdesk.com|Homepage: https://listenandresolve.com|" ./build.py
          sed -i -e "s|launchUrl(Uri.parse('https://rustdesk.com'));|launchUrl(Uri.parse('https://listenandresolve.com'));|" ./flutter/lib/common.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com');|launchUrlString('https://listenandresolve.com');|" ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com/privacy.html')|launchUrlString('https://listenandresolve.com/rustdesk')|" ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e "s|const url = 'https://rustdesk.com/';|const url = 'https://listenandresolve.com';|" ./flutter/lib/mobile/pages/settings_page.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com/privacy.html')|launchUrlString('https://listenandresolve.com/rustdesk')|" ./flutter/lib/mobile/pages/settings_page.dart
          sed -i -e "s|https://rustdesk.com/privacy.html|https://listenandresolve.com/rustdesk|" ./flutter/lib/desktop/pages/install_page.dart
          echo -e "\r🟢 Configuration des URL... OK"
          echo "—————"

          echo "=== LIGNES 85-130 ==="
          sed -n '85,130p' flutter/lib/desktop/pages/desktop_home_page.dart
          
          echo "=== Recherche de LNRUpdateInstallButton ==="
          grep -n "LNRUpdateInstallButton\|LNRWebsite\|Widget LNR" flutter/lib/desktop/pages/desktop_home_page.dart
          
          echo "=== Recherche buildPopupMenu ==="
          grep -n "buildPopupMenu" flutter/lib/desktop/pages/desktop_home_page.dart
          
          echo "=== Nombre total de lignes ==="
          wc -l flutter/lib/desktop/pages/desktop_home_page.dart

          echo "=== LIGNES 125-145 ==="
          sed -n '125,145p' flutter/lib/desktop/pages/desktop_home_page.dart
          
          echo "=== LIGNES 305-360 ==="
          sed -n '305,360p' flutter/lib/desktop/pages/desktop_home_page.dart
#=====# CUSTOMIZATION LISTEN AND RESOLVE (WINDOWS)

      - name: Install LLVM and Clang
        uses: KyleMayes/install-llvm-action@v1
        with:
          version: ${{ env.LLVM_VERSION }}

      - name: Install flutter
        uses: subosito/flutter-action@v2.12.0 #https://github.com/subosito/flutter-action/issues/277
        with:
          channel: "stable"
          flutter-version: ${{ env.FLUTTER_VERSION }}

      # https://github.com/flutter/flutter/issues/155685
      - name: Replace engine with rustdesk custom flutter engine
        run: |
          flutter doctor -v
          flutter precache --windows
          Invoke-WebRequest -Uri https://github.com/rustdesk/engine/releases/download/main/windows-x64-release.zip -OutFile windows-x64-release.zip
          Expand-Archive -Path windows-x64-release.zip -DestinationPath windows-x64-release
          mv -Force windows-x64-release/*  C:/hostedtoolcache/windows/flutter/stable-${{ env.FLUTTER_VERSION }}-x64/bin/cache/artifacts/engine/windows-x64-release/

      - name: Patch flutter
        shell: bash
        run: |
          cp .github/patches/flutter_3.24.4_dropdown_menu_enableFilter.diff $(dirname $(dirname $(which flutter)))
          cd $(dirname $(dirname $(which flutter)))
          [[ "3.24.5" == ${{env.FLUTTER_VERSION}} ]] && git apply flutter_3.24.4_dropdown_menu_enableFilter.diff
          
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.SCITER_RUST_VERSION }}
          targets: ${{ matrix.job.target }}
          components: "rustfmt"

      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: ${{ matrix.job.os }}

      - name: Setup vcpkg with Github Actions binary cache
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: C:\vcpkg
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false

      - name: Install vcpkg dependencies
        env:
          VCPKG_DEFAULT_HOST_TRIPLET: ${{ matrix.job.vcpkg-triplet }}
        run: |
          if ! $VCPKG_ROOT/vcpkg \
            install \
            --triplet ${{ matrix.job.vcpkg-triplet }} \
            --x-install-root="$VCPKG_ROOT/installed"; then
            find "${VCPKG_ROOT}/" -name "*.log" | while read -r _1; do
              echo "$_1:"
              echo "======"
              cat "$_1"
              echo "======"
              echo ""
            done
            exit 1
          fi
          head -n 100 "${VCPKG_ROOT}/buildtrees/ffmpeg/build-${{ matrix.job.vcpkg-triplet }}-rel-out.log" || true
        shell: bash

      - name: replace flutter icons
        continue-on-error: true
        run: |
          cd ./flutter
          flutter pub get
          flutter pub run flutter_launcher_icons
          cd ..

      - name: Build rustdesk
        run: |
          # Windows: build RustDesk
          python3 .\build.py --portable --hwcodec --flutter --vram --skip-portable-pack
          mv ./flutter/build/windows/x64/runner/Release ./rustdesk

          # Download usbmmidd_v2.zip and extract it to ./rustdesk
          Invoke-WebRequest -Uri https://github.com/rustdesk-org/rdev/releases/download/usbmmidd_v2/usbmmidd_v2.zip -OutFile usbmmidd_v2.zip
          Expand-Archive usbmmidd_v2.zip -DestinationPath .
          Remove-Item -Path usbmmidd_v2\Win32 -Recurse
          Remove-Item -Path "usbmmidd_v2\deviceinstaller64.exe", "usbmmidd_v2\deviceinstaller.exe", "usbmmidd_v2\usbmmidd.bat"
          mv -Force .\usbmmidd_v2 ./rustdesk

          # Download printer driver files and extract them to ./rustdesk
          try {
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/rustdesk_printer_driver_v4.zip -OutFile rustdesk_printer_driver_v4.zip
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/printer_driver_adapter.zip -OutFile printer_driver_adapter.zip
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/sha256sums -OutFile sha256sums

            # Check and move the files
            $checksum_driver = (Select-String -Path .\sha256sums -Pattern '^([a-fA-F0-9]{64}) \*rustdesk_printer_driver_v4\.zip$').Matches.Groups[1].Value
            $downloadsum_driver = Get-FileHash -Path rustdesk_printer_driver_v4.zip -Algorithm SHA256
            $checksum_dll = (Select-String -Path .\sha256sums -Pattern '^([a-fA-F0-9]{64}) \*printer_driver_adapter\.zip$').Matches.Groups[1].Value
            $downloadsum_dll = Get-FileHash -Path printer_driver_adapter.zip -Algorithm SHA256
            if ($checksum_driver -eq $downloadsum_driver.Hash -and $checksum_dll -eq $downloadsum_dll.Hash) {
                Write-Output "rustdesk_printer_driver_v4, checksums match, extract the file."
                Expand-Archive rustdesk_printer_driver_v4.zip -DestinationPath .
                mkdir ./rustdesk/drivers
                mv -Force .\rustdesk_printer_driver_v4 ./rustdesk/drivers/RustDeskPrinterDriver
                Expand-Archive printer_driver_adapter.zip -DestinationPath .
                mv -Force .\printer_driver_adapter.dll ./rustdesk
            } elseif ($checksum_driver -ne $downloadsum_driver.Hash) {
                Write-Output "rustdesk_printer_driver_v4, checksums do not match, ignore the file."
            } else {
                Write-Output "printer_driver_adapter.dll, checksums do not match, ignore the file."
            }
          } catch {
              Write-Host "Ingore the printer driver error."
          }

      - name: find Runner.res
        # Windows: find Runner.res (compiled from ./flutter/windows/runner/Runner.rc), copy to ./Runner.res
        # Runner.rc does not contain actual version, but Runner.res does
        continue-on-error: true
        shell: bash
        run: |
          runner_res=$(find . -name "Runner.res");
          if [ "$runner_res" == "" ]; then
            echo "Runner.res: not found";
          else
            echo "Runner.res: $runner_res";
            cp $runner_res ./libs/portable/Runner.res;
            echo "list ./libs/portable/Runner.res";
            ls -l ./libs/portable/Runner.res;
          fi

      - name: Download RustDeskTempTopMostWindow artifacts
        uses: actions/download-artifact@master
        if: ${{ inputs.upload-artifact }}
        with:
          name: topmostwindow-artifacts
          path: "./rustdesk"

      - name: Upload unsigned
        if: env.UPLOAD_ARTIFACT == 'true'
        uses: actions/upload-artifact@master
        with:
          name: rustdesk-unsigned-windows-${{ matrix.job.arch }}
          path: rustdesk

### CUSTOMIZATION LISTEN AND RESOLVE
      - name: Sign Rustdesk files (Azure Trusted Signing)
        if: env.UPLOAD_ARTIFACT == 'true'
        uses: azure/trusted-signing-action@v0.5.1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          trusted-signing-account-name: ${{ secrets.AZURE_CODE_SIGNING_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERT_PROFILE_NAME }}
          files-folder: ./rustdesk
          files-folder-filter: exe
### CUSTOMIZATION LISTEN AND RESOLVE

      - name: Build self-extracted executable
        shell: bash
        if: env.UPLOAD_ARTIFACT == 'true'
        run: |
          sed -i '/dpiAware/d' res/manifest.xml
          pushd ./libs/portable
          pip3 install -r requirements.txt
          python3 ./generate.py -f ../../rustdesk/ -o . -e ../../rustdesk/rustdesk.exe
          popd
          mkdir -p ./SignOutput
          mv ./target/release/rustdesk-portable-packer.exe ./SignOutput/rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.exe

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Build msi
        if: env.UPLOAD_ARTIFACT == 'true'
        run: |
          pushd ./res/msi
          python preprocess.py --arp -d ../../rustdesk
          nuget restore msi.sln
          msbuild msi.sln -p:Configuration=Release -p:Platform=x64 /p:TargetVersion=Windows10
          mv ./Package/bin/x64/Release/en-us/Package.msi ../../SignOutput/rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.msi
          sha256sum ../../SignOutput/rustdesk-*.msi

### CUSTOMIZATION LISTEN AND RESOLVE
      - name: Sign RustDesk self-extract (Azure Trusted Signing)
        if: env.UPLOAD_ARTIFACT == 'true'
        uses: azure/trusted-signing-action@v0.5.1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          trusted-signing-account-name: ${{ secrets.AZURE_CODE_SIGNING_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERT_PROFILE_NAME }}
          files-folder: ./SignOutput
          files-folder-filter: exe,msi
### CUSTOMIZATION LISTEN AND RESOLVE

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        if: env.UPLOAD_ARTIFACT == 'true'
        with:
          prerelease: false
          make_latest: true
          tag_name: ${{ env.TAG_NAME }}
          name: ${{ env.VERSION }}
          files: |
            ./SignOutput/rustdesk-*.msi
            ./SignOutput/rustdesk-*.exe

#==================================================
#
# MacOS
#
#==================================================
  
